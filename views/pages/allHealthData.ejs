<%- include ('../partials/header'); %>

<main class="main container">   
    <div class="row">
        <% data.forEach(item => { %>
            <div class="col-md-4">
                <a href="/allHealthData/<%= item.id %>">
                    <p><%= item.long_name %></p>
                    <hr>
                </a>
            </div>
        <% }) %>
    </div>
</main>

<%- include ('../partials/header'); %>